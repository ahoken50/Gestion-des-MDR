rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Règles pour les compteurs - Accès public pour permettre la numérotation
    // Protection: Validation du type et interdiction de suppression
    match /counters/{document} {
      allow read: if true;
      allow create, update: if request.resource.data.value is number;
      allow delete: if false;
    }
    
    // Règles pour les demandes de ramassage - Accès public
    match /pickupRequests/{requestId} {
      // Lecture: accès public
      allow read: if true;
      
      // Création: accès public avec validation des données
      allow create: if request.resource.data.requestNumber is number
        && request.resource.data.contactName is string
        && request.resource.data.contactPhone is string
        && request.resource.data.status in ['pending', 'completed']
        && request.resource.data.items is list
        && request.resource.data.items.size() > 0;
      
      // Mise à jour: accès public
      // Protection: On s'assure que le numéro de requête reste un nombre
      allow update: if request.resource.data.requestNumber is number;
      
      // Suppression: interdite pour le moment (protection des données)
      allow delete: if false;
    }
    
    // Règles pour l'inventaire - Accès public
    // Protection: Validation des types et interdiction de suppression
    match /inventory/{itemId} {
      allow read: if true;
      allow create, update: if request.resource.data.name is string
        && request.resource.data.quantity is number
        && request.resource.data.location is string;
      allow delete: if false;
    }
  }
}

// Règles pour Firebase Storage - Accès public
service firebase.storage {
  match /b/{bucket}/o {
    // Images des demandes - Accès public
    match /requests/{requestId}/{allPaths=**} {
      // Lecture: accès public
      allow read: if true;
      
      // Écriture: accès public avec limite de taille (10MB)
      allow write: if request.resource.size < 10 * 1024 * 1024;
    }
  }
}
